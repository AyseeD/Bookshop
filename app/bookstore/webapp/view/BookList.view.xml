<mvc:View
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
    <List items="{/ListOfBooks}">
        <StandardListItem 
            title="{title}"
            description="{author}"
            info="{price} {currency}"
            icon="{image}"/>
    </List>
</mvc:View>